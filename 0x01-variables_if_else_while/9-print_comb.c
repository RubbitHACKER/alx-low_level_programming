#include<stdio>
/**
 * main - A program that prints the numbers from 0 to 9 then alphabit
 *
 * Return: Always 0 (success)
 */
int main(void)
{
	int i;

	for (i = 48; i < 58; i++)
{
	putchar(i);
{
	for (i = 44; i == 44; i++)
	putchar(i);
}
putchar('36');
putchar('\n');
return (0);
}
