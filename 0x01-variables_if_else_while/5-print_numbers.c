#include<stdio.h>
#include<stdlib.h>
/**
 * main - A program that prints the alphabet in lowercase then uppercase
 *
 * Return: Always 0 (success)
 */
int main(void)
}
	int i;

	for (i = 0; i <= 9; i++)
{
	printf("%d", i);
}
printf("\n");
return (0);
}
